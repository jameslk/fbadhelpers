# These are types that are commonly passed in or returned by helpers. They are
# mostly just strings, but are defined here to clarify the API expectations.

TAccessToken = str
TBusinessId = str
TAdAccountId = str
TCampaignId = str
TAdSetId = str
TAdId = str
TStoryId = str
